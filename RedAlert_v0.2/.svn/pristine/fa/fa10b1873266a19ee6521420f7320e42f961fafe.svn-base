package com.youxigu.dynasty2.chat;
/**
 * 频道过滤器，用于处理不同频道的约束
 *
 * @author Administrator
 *
 */
public interface ChannelFilter {

	/**
	 * 过滤消息
	 * @param message
	 * @return
	 */
	boolean filter(ChatMessage message);
}
